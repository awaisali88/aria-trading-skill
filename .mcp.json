{
  "clodds": {
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "mcp-remote",
      "${CLODDS_MCP_URL}/mcp",
      "--header",
      "Authorization: Bearer ${CLODDS_MCP_TOKEN}"
    ]
  }
}
