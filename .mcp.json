{
  "aria": {
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "mcp-remote",
      "${ARIA_MCP_URL}/mcp",
      "--header",
      "Authorization: Bearer ${ARIA_MCP_TOKEN}"
    ]
  }
}
